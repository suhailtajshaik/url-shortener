# MongoDB Configuration (choose one method)
# Method 1: Direct connection string (for local development)
MONGO_URI=mongodb://localhost:27017/url-shortener

# Method 2: MongoDB Atlas with username/password (recommended for production)
# If MONGO_URI is not set, these will be used to build the Atlas connection string
MONGO_USERNAME=admin
MONGO_PASSWORD=Suhail@123!
MONGO_CLUSTER=default.schqzct.mongodb.net
MONGO_APP_NAME=default

# Server configuration (optional - defaults provided)
NODE_PROTOCOL=http
NODE_HOSTNAME=localhost
NODE_PORT=3000
NODE_ENV=development
MOCK_FLAG=false

# Default URL expiration in hours (optional - default: 720 hours = 30 days)
DEFAULT_URL_EXPIRATION_HOURS=720
